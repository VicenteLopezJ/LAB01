apiVersion: v1
kind: Service
metadata:
  name: ms-productos-lb
  namespace: microservicios
  labels:
    app: ms-productos
spec:
  selector:
    app: ms-productos
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
  type: LoadBalancer
