apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-pedidos
  namespace: microservicios
  labels:
    app: ms-pedidos
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ms-pedidos
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: ms-pedidos
        version: "1.0"
    spec:
      containers:
        - name: ms-pedidos
          image: llampier2/ms-pedidos:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8082
              protocol: TCP
          env:
            - name: SERVER_PORT
              value: "8082"
          envFrom:
            - configMapRef:
                name: ms-config
            - secretRef:
                name: ms-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 25
            periodSeconds: 5
            failureThreshold: 3
            timeoutSeconds: 3
